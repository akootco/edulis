group = project.group
version = project.version

repositories {
    maven {
        name = "jsinco"
        url = "https://repo.jsinco.dev/releases"
    }
}

dependencies {
    compileOnly project(":bluefox")
    compileOnly project(":plushies")
    compileOnly 'com.dre.brewery:BreweryX:3.6.2'
}